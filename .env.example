# En Netlify (sitio de prueba / presentación):
#   Build: npm run build:demo
#   (o variable VITE_DEMO=true + build; el script build:demo usa --mode demo y .env.demo)
#
# Producción operativa: npm run build  (sin modo demo)
